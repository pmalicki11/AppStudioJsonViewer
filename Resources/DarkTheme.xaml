<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:search="clr-namespace:ICSharpCode.AvalonEdit.Search;assembly=ICSharpCode.AvalonEdit">

    <!-- ============================ Palette ============================ -->
    <SolidColorBrush x:Key="Bg.Window"       Color="#1E1E1E" />
    <SolidColorBrush x:Key="Bg.Panel"        Color="#252526" />
    <SolidColorBrush x:Key="Bg.PanelAlt"     Color="#2A2A2B" />
    <SolidColorBrush x:Key="Bg.Control"      Color="#333337" />
    <SolidColorBrush x:Key="Bg.ControlHover" Color="#3E3E42" />
    <SolidColorBrush x:Key="Fg.Default"      Color="#D4D4D4" />
    <SolidColorBrush x:Key="Fg.Muted"        Color="#9D9D9D" />
    <SolidColorBrush x:Key="Border.Default"  Color="#3F3F46" />
    <SolidColorBrush x:Key="Accent"          Color="#0E639C" />
    <SolidColorBrush x:Key="Accent.Hover"    Color="#1177BB" />
    <SolidColorBrush x:Key="Selection"       Color="#094771" />
    <SolidColorBrush x:Key="Link"            Color="#4FC1FF" />

    <!-- ============================ Window ============================ -->
    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource Bg.Window}" />
        <Setter Property="Foreground" Value="{StaticResource Fg.Default}" />
    </Style>

    <!-- ============================ TextBlock ========================= -->
    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Fg.Default}" />
    </Style>

    <!-- ============================ TextBox =========================== -->
    <Style TargetType="TextBox">
        <Setter Property="Foreground" Value="{StaticResource Fg.Default}" />
        <Setter Property="Background" Value="{StaticResource Bg.Control}" />
        <Setter Property="BorderBrush" Value="{StaticResource Border.Default}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CaretBrush" Value="{StaticResource Fg.Default}" />
        <Setter Property="SelectionBrush" Value="{StaticResource Selection}" />
        <Setter Property="Padding" Value="6,8" />
        <Setter Property="MinWidth" Value="200" />
        <Setter Property="MinHeight" Value="36" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="3">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Margin="{TemplateBinding Padding}"
                                      VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter Property="BorderBrush" Value="{StaticResource Accent.Hover}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============================ Button =========================== -->
    <Style TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource Fg.Default}" />
        <Setter Property="Background" Value="{StaticResource Bg.Control}" />
        <Setter Property="BorderBrush" Value="{StaticResource Border.Default}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="10,4" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="3" SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background"
                                    Value="{StaticResource Bg.ControlHover}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============================ ProgressBar ====================== -->
    <Style TargetType="ProgressBar">
        <Setter Property="Background" Value="{StaticResource Bg.Control}" />
        <Setter Property="Foreground" Value="{StaticResource Accent.Hover}" />
        <Setter Property="BorderBrush" Value="{StaticResource Border.Default}" />
    </Style>

    <!-- ============================ DataGrid ========================= -->
    <Style TargetType="DataGrid">
        <Setter Property="Background" Value="{StaticResource Bg.Panel}" />
        <Setter Property="Foreground" Value="{StaticResource Fg.Default}" />
        <Setter Property="BorderBrush" Value="{StaticResource Border.Default}" />
        <Setter Property="RowBackground" Value="{StaticResource Bg.Panel}" />
        <Setter Property="AlternatingRowBackground" Value="{StaticResource Bg.PanelAlt}" />
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource Border.Default}" />
        <Setter Property="VerticalGridLinesBrush" Value="{StaticResource Border.Default}" />
        <Setter Property="RowHeaderWidth" Value="0" />
    </Style>

    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{StaticResource Bg.Control}" />
        <Setter Property="Foreground" Value="{StaticResource Fg.Default}" />
        <Setter Property="BorderBrush" Value="{StaticResource Border.Default}" />
        <Setter Property="BorderThickness" Value="0,0,1,1" />
        <Setter Property="Padding" Value="8,6" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style TargetType="DataGridRow">
        <Setter Property="Background" Value="{StaticResource Bg.Panel}" />
        <Setter Property="Foreground" Value="{StaticResource Fg.Default}" />
    </Style>

    <Style TargetType="DataGridCell">
        <Setter Property="Foreground" Value="{StaticResource Fg.Default}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource Selection}" />
                <Setter Property="Foreground" Value="{StaticResource Fg.Default}" />
                <Setter Property="BorderBrush" Value="{StaticResource Selection}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ============================ ComboBox ========================= -->
    <Style TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="{StaticResource Fg.Default}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Padding" Value="8,5" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Bg.ControlHover}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Accent}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="Foreground" Value="{StaticResource Fg.Default}" />
        <Setter Property="Background" Value="{StaticResource Bg.Control}" />
        <Setter Property="BorderBrush" Value="{StaticResource Border.Default}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="8,5" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="24" />
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  Margin="{TemplateBinding Padding}"
                                                  VerticalAlignment="Center"
                                                  IsHitTestVisible="False" />
                                <Path Grid.Column="1"
                                      Data="M 0 0 L 4 4 L 8 0"
                                      Stroke="{StaticResource Fg.Muted}"
                                      StrokeThickness="1.5"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      IsHitTestVisible="False" />
                                <ToggleButton Grid.ColumnSpan="2"
                                              IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                              Background="Transparent"
                                              BorderThickness="0"
                                              Focusable="False" />
                            </Grid>
                        </Border>
                        <Popup x:Name="PART_Popup"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True"
                               Placement="Bottom"
                               PlacementTarget="{Binding ElementName=Bd}"
                               MinWidth="{TemplateBinding ActualWidth}"
                               StaysOpen="False">
                            <Border Background="{StaticResource Bg.Panel}"
                                    BorderBrush="{StaticResource Border.Default}"
                                    BorderThickness="1"
                                    CornerRadius="3">
                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <ItemsPresenter />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Bg.ControlHover}" />
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Accent.Hover}" />
                        </Trigger>
                        <Trigger Property="IsDropDownOpen" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Accent.Hover}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============================ Hyperlink ======================== -->
    <Style TargetType="Hyperlink">
        <Setter Property="Foreground" Value="{StaticResource Link}" />
    </Style>

    <!-- ============================ AvalonEdit SearchPanel =========== -->
    <Style TargetType="{x:Type search:SearchPanel}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type search:SearchPanel}">
                    <Border Background="{StaticResource Bg.Panel}"
                            BorderBrush="{StaticResource Border.Default}"
                            BorderThickness="1,0,0,1"
                            Padding="6,4"
                            VerticalAlignment="Top"
                            HorizontalAlignment="Right">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBox x:Name="PART_searchTextBox"
                                     MinWidth="200" MinHeight="26"
                                     Padding="6,3"
                                     Foreground="{StaticResource Fg.Default}"
                                     Background="{StaticResource Bg.Control}"
                                     BorderBrush="{StaticResource Border.Default}"
                                     BorderThickness="1"
                                     CaretBrush="{StaticResource Fg.Default}"
                                     SelectionBrush="{StaticResource Selection}"
                                     VerticalContentAlignment="Center">
                                <TextBox.Text>
                                    <Binding Path="SearchPattern"
                                             RelativeSource="{RelativeSource TemplatedParent}"
                                             UpdateSourceTrigger="PropertyChanged">
                                        <Binding.ValidationRules>
                                            <ExceptionValidationRule />
                                        </Binding.ValidationRules>
                                    </Binding>
                                </TextBox.Text>
                            </TextBox>
                            <Button Margin="4,0,0,0" MinHeight="26" Padding="8,0"
                                    Command="{x:Static search:SearchCommands.FindPrevious}"
                                    ToolTip="{Binding Localization.FindPreviousText, RelativeSource={RelativeSource TemplatedParent}}">
                                <Image Source="pack://application:,,,/ICSharpCode.AvalonEdit;component/Search/prev.png"
                                       Width="16" Height="16" RenderOptions.BitmapScalingMode="NearestNeighbor" />
                            </Button>
                            <Button Margin="4,0,0,0" MinHeight="26" Padding="8,0"
                                    Command="{x:Static search:SearchCommands.FindNext}"
                                    ToolTip="{Binding Localization.FindNextText, RelativeSource={RelativeSource TemplatedParent}}">
                                <Image Source="pack://application:,,,/ICSharpCode.AvalonEdit;component/Search/next.png"
                                       Width="16" Height="16" RenderOptions.BitmapScalingMode="NearestNeighbor" />
                            </Button>
                            <search:DropDownButton Margin="4,0,0,0" MinHeight="26" MinWidth="26" Padding="4,0"
                                                   Cursor="Hand">
                                <search:DropDownButton.DropDownContent>
                                    <Popup x:Name="PART_dropdownPopup" StaysOpen="False">
                                        <Border Background="{StaticResource Bg.Panel}"
                                                BorderBrush="{StaticResource Border.Default}"
                                                BorderThickness="1" Padding="10,8">
                                            <StackPanel Orientation="Vertical">
                                                <CheckBox Margin="0,0,0,6"
                                                          IsChecked="{Binding MatchCase, RelativeSource={RelativeSource AncestorType={x:Type search:SearchPanel}}}"
                                                          Content="{Binding Localization.MatchCaseText, RelativeSource={RelativeSource AncestorType={x:Type search:SearchPanel}}}" />
                                                <CheckBox Margin="0,0,0,6"
                                                          IsChecked="{Binding WholeWords, RelativeSource={RelativeSource AncestorType={x:Type search:SearchPanel}}}"
                                                          Content="{Binding Localization.MatchWholeWordsText, RelativeSource={RelativeSource AncestorType={x:Type search:SearchPanel}}}" />
                                                <CheckBox IsChecked="{Binding UseRegex, RelativeSource={RelativeSource AncestorType={x:Type search:SearchPanel}}}"
                                                          Content="{Binding Localization.UseRegexText, RelativeSource={RelativeSource AncestorType={x:Type search:SearchPanel}}}" />
                                            </StackPanel>
                                        </Border>
                                    </Popup>
                                </search:DropDownButton.DropDownContent>
                            </search:DropDownButton>
                            <Button Margin="4,0,0,0" MinHeight="26" Padding="8,0"
                                    Command="{x:Static search:SearchCommands.CloseSearchPanel}"
                                    ToolTip="Close (Esc)"
                                    Content="✕" />
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
